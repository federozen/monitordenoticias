# Instrucciones

Comenzar por `MIGRACION_RAPIDA.md`. La configuracion completa, los Secrets y las variables estan en `README.md`.

La primera prueba debe realizarse con `AUTOMATION_ENABLED=false` y `AGENT_TELEGRAM_MODE=off`.
